<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="Button">
        <item name="android:layout_width">@dimen/tile_size</item>
        <item name="android:layout_height">@dimen/tile_size</item>
        <item name="android:layout_margin">@dimen/tile_margin</item>
        <item name="android:src">@drawable/button</item>
        <item name="android:background">#80FFFFFF</item>
        <item name="android:padding">@dimen/tile_padding</item>
        <item name="android:scaleType">centerCrop</item>

    </style>

    <style name="Buttonlan">
        <item name="android:layout_width">@dimen/tile_sizelan</item>
        <item name="android:layout_height">@dimen/tile_sizelan</item>
        <item name="android:layout_margin">@dimen/tile_marginlan</item>
        <item name="android:src">@drawable/button</item>
        <item name="android:background">#80FFFFFF</item>
        <item name="android:padding">@dimen/tile_padding</item>
        <item name="android:scaleType">centerCrop</item>

    </style>

</resources>
